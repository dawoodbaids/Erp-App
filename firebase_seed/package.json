{
  "name": "firebase-seed",
  "version": "1.0.0",
  "private": true,
  "description": "Idempotent seed script for the ERP Firebase project. Never deletes or overwrites existing documents.",
  "main": "db.js",
  "scripts": {
    "seed": "node db.js",
    "seed:dry-run": "node db.js --dry-run"
  },
  "dependencies": {
    "firebase-admin": "^14.2.0"
  }
}
